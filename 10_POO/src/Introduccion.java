
public class Introduccion {

	public static void main(String[] args) {
		/*
		 * En POO(Programacion Orientada a Objetos) las caracteristicas se denominan en propiedades y las acciones en metodos.
		 * 
		 * Cada uno de los objetos en la programacion colabora con otros objetos formando las relaciones.
		 * 
		 * El estado de un objeto en un momento dado viene determinado por el conjunto de valores de sus propiedades. 
		 * El estado del objeto está cambiando constantemente por medio de los metodos o a partir de los mensajes que recibe
		 * de otro objetos emisor. 
		 * 
		 * METODOS
		 * 
		 * Existen varios tipos de metodos:
		 * 
		 * 		* De acceso a las propiedades: Acceden a una propiedad del objeto con el fin de obtener su valor 
		 * 										(metodos GET) o modificar su valor (metodos SET).
		 * 
		 * 		* De seleccion o consulta: Acceden al estado de un objeto (valores de sus propiedades) sin modificar su valor. 
		 * 									Para un objeto de la clase Coche, podría ser un metodo llamado consultar() que devuelve 
		 * 									cadena con todos los datos del coche. 
		 * 
		 * 		* Asignan valores a las propiedades del objeto, es decir, establecen su estado.
		 * 
		 * IDENTIDAD DE UN OBJETO
		 * La identidad de un objeto es la propiedad que le permite distinguirse de otro objeto del mismo tipo dentro
		 * de una coleccion. 
		 * 
		 * Todas las comunicaciones son efectuadas a traves de mensajes que son enviados o recibidos a traves de los metodos objetos.
		 * 
		 * Una aplicacion que utilizael paradigma de orientacion a objetos siempre sera modular, ya que la logixa de cada una de las tareas
		 * requeridas por el sistema estara encapsulada en una clase desarrollada especificamente para responder a esa necesidad. 
		 * 
		 * 
		 * 				CONCEPTO DE ABSTRACION
		 * 				----------------------
		 * Para comprender mejor el concepto de abstracion en programacion, piensa antes en su significado filosofico. 
		 * Cuando decimos "Me abstraigo del ruido para centrarme en esta tarea", significa que me aislo completamente del entornor y de las posibles
		 * distracciones para centrarme exclusivamente en dicha tarea. 
		 * En programacion ocurre exactamente lo mismo, podemos aislar un elemento podemos aislar un elemento de todo
		 * el resto de elementos que le acompañan para centrarnos exclusivamente en el.
		 * En POO podemos ocultar los detalles complejos de implementacion de un objeto para centrarnos exclusivamente en las acciones que 
		 * podemos ejecutar con dicho objeto. Esto es abstraccion. 
		 * 
		 * LAS PROPIEDADES SON VARIABLES DE MEMORIA
		 * ----------------------------------------
		 * Las propiedades o caracteristicas de una clase se definen como variables de memoria. Una variable representa un espacioen la memoria 
		 * RAM al que accedemos por medio de un identificador o nombre de la variable.
		 * 
		 * FORMATO DE DECLARACION DE VARIABLES: 
		 * modificadores tipo identificador;
		 * 
		 * 		*Los modificadores son palabras que configuran el metodo de acceso a la variable (public, private, protecte, static, etc.)
		 * 		*El tipo se refier al rango de valores que pueden guardarse en la variable (int, floa, double, String, etc.)
		 * 		*El identificador es el nombre de la variable. 
		 * 
		 * LOS METODO SON FUNCIONES
		 * ------------------------
		 * Los metodos de las clases se implementan como funciones. Una funcion tiene un nombre y agrupa a un conjunto de lineas de coigo. 
		 * Puede recibir argumentos y retornar un valor. Cuando una funcion JAVA no devuelve ningun valor se indica como el tipo de 
		 * retornor la palabra reservada void.
		 * 
		 *  	* Los modificadores Los modificadores son palabras que configuran el metodo de acceso a la funcion (public, private, protecte, static, etc.)
		 *  	* El tipo se refier al tipo de retornor que devolvera la funcion. Se especifica void si la funcion no devuelve nada. 
		 *  	* El identificador es el nombre de la funcion. 
		 *  	* El identificador es el nombre de la funcion. 
		 *  	* Los argumentos son un conjunto de valores de entrada que seran utilizados en el interior de la funcion. 
		 *  	* La sentencia return finaliza la ejecucion de la funcion devolviendo el valor especificado. 
		 */

	}

}
